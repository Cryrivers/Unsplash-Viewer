<img src="{{url}}"
     class="picture-detail-viewer__photo"
     data-picture-id="{{id}}"
     style="transform: scale({{pictureScale}}) rotate({{pictureAngle}}deg) translateX({{pictureX}}px) translateY({{pictureY}}px);">