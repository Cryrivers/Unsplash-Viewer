import platform from 'wora/utils/platform';
import { module, test } from 'qunit';

module('Unit | Utility | platform');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = platform();
  assert.ok(result);
});
